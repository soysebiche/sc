# Optional local overrides. Copy to .env.local if needed.
# No variables are required to run or build the app.

# Google Analytics 4 Measurement ID (loaded only after explicit visitor consent)
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
